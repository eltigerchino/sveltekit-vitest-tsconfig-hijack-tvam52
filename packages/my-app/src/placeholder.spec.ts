import { describe, expect, it, test } from "vitest";

describe("bogus test", () => {
  test("placeholder", () => {
    // This is a placeholder test so running `vitest` does not complain about
    // missing tests.
    expect(true).toBe(true);
  });
});
